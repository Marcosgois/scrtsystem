# Dependências e artefatos locais
node_modules/
.env
.DS_Store
~$*.xlsx

# Banco MongoDB local (dados da aplicação) — só a pasta data/ da raiz.
# (Ancorado com "/" para não pegar src/data/, que guarda referências versionadas.)
/data/

# ─────────────────────────────────────────────────────────────────────────
# DADOS REAIS DE CLIENTES — não versionar (privacidade / dados de terceiros)
# SCRTs, inventários e planilhas de CAIXA, BB, BRB e Itaú ficam só na máquina.
# ─────────────────────────────────────────────────────────────────────────
SCRT/
mlc/
scrt/
*.xlsx
*.htm

# Mockups/versões antigas com dados reais embutidos
consumo_tfp_caixa_v0.7.html
app_inventario_esw_v0.12.html

# Pasta de inventário: relatórios reais de clientes ficam só na máquina.
# (A página do inventário agora é public/inventario.html, versionada direto.)
Inventario/
inventario/
